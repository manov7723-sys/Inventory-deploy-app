apiVersion: v1
kind: Service
metadata:
  name: inventory-deploy-app
  namespace: storefront-dev-1
  labels:
    app.kubernetes.io/name: inventory-deploy-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inventory-deploy-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
