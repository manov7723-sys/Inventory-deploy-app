apiVersion: v1
kind: Service
metadata:
  name: inventory-deploy-app-orders
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: inventory-deploy-app-orders
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inventory-deploy-app-orders
  ports:
    - name: http
      port: 80
      targetPort: 8080
