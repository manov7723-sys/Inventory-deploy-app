apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: inventory-deploy-app-catalog
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: inventory-deploy-app-catalog
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-deploy-app-catalog
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
